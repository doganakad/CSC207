package sprites;

public class CleanHallway extends Sprite {

	/**
	 * Initializes a new CleanHallway with its symbol, row number and column number.
	 * @param symbol a character which belongs specifically to a CleanHallway
	 * @param row the row number of the location
	 * @param column the column number of the location
	 */
	public CleanHallway(char symbol, int row, int column){
		super(symbol, row, column);
	}
	
}
